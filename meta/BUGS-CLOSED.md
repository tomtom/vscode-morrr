## Closed

1. Also update the params variable when the value has changed. #closed/2026-05-04
2. Evaluate params only when the editor window is visible and has focus. #closed/2026-05-06
3. Provide a command that can be selected from the command palette and be mapped to a key shortcut, to invoke loading the params from yaml metadata. #closed/2026-05-12
